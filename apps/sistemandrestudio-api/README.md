# sistemandrestudio-api

Este workspace contém apenas a demonstração CLI da máquina editorial:

```bash
npm run demo
```

Não existe API HTTP, servidor, porta aberta ou integração externa nesta etapa.
O nome do workspace preserva a fronteira planejada para a aplicação futura.
